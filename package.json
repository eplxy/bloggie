{
  "name": "bloggie",
  "version": "1.0.0",
  "scripts": {
      "build": "yarn run build --prefix client",
      "start": "node api/index.js",
      "heroku-postbuild": "yarn install --prefix client && yarn run build --prefix client"
  }
}